# Copie este archivo a `.env` y complete la clave del proveedor que vaya a usar.
# Solo hace falta para la corrida real. Las pruebas corren sin clave ni conexión.

# ── Por defecto · Google Gemini ─────────────────────────────────────────────
# Clave gratuita, sin tarjeta, en Google AI Studio.
PROVIDER=google
MODEL=gemini-3.1-flash-lite
GOOGLE_API_KEY=

# ── Respaldo de sala · OpenAI ───────────────────────────────────────────────
# Más fiable que la capa gratuita, pero concentra el consumo en una cuenta.
# Para cambiar: PROVIDER=openai y MODEL=gpt-5.4-mini
# OPENAI_API_KEY=

# ── Opcionales ──────────────────────────────────────────────────────────────
# El evaluador puede usar otro proveedor que el agente.
# JUDGE_PROVIDER=
# JUDGE_MODEL=

# Tope de vueltas del bucle. Bajarlo a 2 demuestra el corte: con 3 el modelo
# alcanza a terminar, porque pide varias cotizaciones en un mismo paso.
# MAX_STEPS=12
